# Regenerable shortcut output (run event-log/shortcuts/generate-log-event.py to recreate)
event-log/shortcuts/generated/

# macOS
.DS_Store

# Node
node_modules/

# package-lock.json IS committed as of the sheets/ts workspace: `npm ci` in
# GitHub Actions requires it, and there are real dependencies now.
# See docs/plans/typescript-port.md.
